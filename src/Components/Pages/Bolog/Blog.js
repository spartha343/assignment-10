import React from 'react';

const Blog = () => {
    return (
        <div className='p-5 m-5'>
            <h1>What is the difference between authentication and authorization?</h1>
            <h4 className='pb-4'>authentication is the process of verifying who someone is, and authorization is the process of verifying what
                specific applications, files, and data a
                user has access to.</h4>
            <h1>why use firebase?</h1>
            <h4>
                We use firebase because it allows.Create Application without backend server.No need extra money spent for backend server.Quick display data in the application.
                You can provide any social networking login with very few lines code
            </h4>
        </div>
    );
};

export default Blog;